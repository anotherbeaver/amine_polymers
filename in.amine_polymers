print "Starting in.amine_polymers"

# simulation parameters
units lj    #  Lennard-Jones units, essentially unitless
dimension 3
boundary p p p
atom_style molecular 
log logs/log.amine_polymers 

# initial configuration
read_data data/data.particles

replicate 5 10 10

write_data data/data.replicated_particles

# group definitions
group monomer type 1
group amine type 2

# bond potentials (harmonic)
bond_style harmonic
bond_coeff 1 50.0 1.0 # k, r0 # backbone stiffness
# k is derived from Raffaeilli and Ellenbroek, Soft Matter 2021
# "...polymer backbones have a stiffness of 50e/b^2..."
bond_coeff 2 100 0.45 # stiff spring on patches

# interatomic potentials (non-bonded)
pair_style hybrid/overlay lj/cut 1.12246 gauss 2.0
pair_coeff 1 1 lj/cut 1.0 1.0 1.12246 # WCA between backbone beads
pair_coeff 1 2 lj/cut 0.0 1.0 1.12246 # no WCA between backbone and amine patches
pair_coeff 2 2 lj/cut 0.0 1.0 1.12246 # no WCA between amine patches
pair_coeff 2 2 gauss 50.0 13.8504155125 2.0 # Gaussian attraction between amine patches


# neighbor list (not too sure why we need this)
neighbor 0.3 bin
neigh_modify every 1 delay 0 check yes

# Energy minimization to relax overlaps (is this necessary?)
min_style cg             # conjugate gradient (good default)
minimize 1.0e-6 1.0e-8 10000 100000

# track transient bonds
compute 1 amine pair/local dist 
dump 1 amine local 10 data/amine_bonds.dat c_1

# initial velocities
velocity all create 0.6 1 # initial temperature 0.6, random seed 1
fix 1 all nve # integrate with NVE ensemble (maybe changed to nvt later)

reset_timestep 0
dump 2 all atom 10 data/dump.polymer.lammpstrj
timestep 0.005 # could be 0.001, Raffaeilli and Ellenbroek, Soft Matter 2021
thermo 100
run 10000


print "Ending in.amine_polymers"
